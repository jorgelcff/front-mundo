import { OnlyLetterAndNumberDirective } from './only-letter-and-number.directive';

describe('OnlyLetterAndNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyLetterAndNumberDirective();
    expect(directive).toBeTruthy();
  });
});
